{
  "name": "the-fifth-stone",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test:countdown": "node --experimental-strip-types --test tests/countdown.test.ts",
    "test:prerender": "node --test tests/prerender.test.mjs",
    "test": "npm run test:countdown && npm run test:prerender",
    "build": "tsc && vite build && node scripts/prerender.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0"
  }
}
